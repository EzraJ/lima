#pragma once

/*
Basic file structure(for now)
Game
  init.as
  levels
    manifests
  scripts
    traits(ill explain later)
  items(not to be confused with in-game items
  dynamic
*/

namespace lima{
	namespace core{
		namespace engine{
			
		}
	}
}